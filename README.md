##I am a DSO API to get model coefficients based on data provided by the API user
just input yout data as a comma separated string of numbers at {{root_url}}/get-coefficients
and you will get back your coefs !
